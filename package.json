{
  "name": "mspp-admin-concierge",
  "version": "1.0.0",
  "description": "Microsoft Power Platform Administrator Compliance Checklist and Monitoring Dashboard",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": [
    "power-platform",
    "compliance",
    "checklist",
    "administration",
    "dashboard"
  ],
  "author": "Power Platform Admin Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
